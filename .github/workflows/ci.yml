# CI exists because of a specific, measured failure: on 2026-08-15 gBASIC did not
# build AT ALL on Ubuntu 24.04 LTS or 25.04 when the optional dependencies were
# present, and a 216-example / 303-negative-case suite did not notice, because a
# `#if HAVE_*` guard and a `pkg-config --exists` check are both blind to the
# configuration they did not select. Three separate defects had accumulated that
# way, one of them for three weeks.
#
# So the matrix is not about breadth for its own sake. Each job is a
# configuration that was actually broken and could not be seen from a developer
# machine:
#
#   full      every optional module enabled -- catches the link/compile errors
#             that only appear when a module is COMPILED, e.g. the GTK 3 gui
#             module, which no local build here compiles at all.
#   minimal   nothing optional -- catches the opposite: code that assumes a
#             library is present, and suites that fail cases for correctly
#             reporting a module is absent (34 of 182 examples once did).
#   older     the current LTS, whose library VERSIONS differ from a dev box.
#             gi_repository_dup_default and libxml2's const change were both
#             version defects, not missing-dependency defects.
name: CI

on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  full:
    name: all optional modules (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          # NOTE: libcrypt-dev, not libxcrypt-dev, and libgirepository-2.0-dev,
          # not libgirepository1.0-dev. Both wrong names were in check-deps.sh
          # until 2026-08-15; because --install issues a single apt-get, one bad
          # name meant nothing was installed at all.
          sudo apt-get install -y -qq \
            build-essential bison pkg-config \
            zlib1g-dev libxml2-dev libsqlite3-dev libpq-dev \
            libcurl4-openssl-dev libssl-dev libcrypt-dev \
            libgtk-3-dev libgirepository-2.0-dev libglib2.0-dev

      - name: Report what was detected
        run: ./tools/check-deps.sh

      - name: Build everything
        run: make dev

      - name: Version
        run: ./gbasic --version

      - name: Run every suite
        run: |
          status=0
          for s in tests/run_*.sh; do
            printf '\n===== %s =====\n' "$s"
            if ! timeout 2400 bash "$s"; then
              printf '::error::%s failed\n' "$s"
              status=1
            fi
          done
          printf '\n===== tests/lsp/run_lsp.sh =====\n'
          timeout 600 bash tests/lsp/run_lsp.sh || status=1
          exit "$status"

  # TOOLCHAIN AGE, which is a DIFFERENT AXIS from everything above and was not
  # covered by any of it.
  #
  # On 2026-09-20 gBASIC did not compile on Ubuntu 22.04 at all: src/eval.c uses
  # va_list/va_start/va_end nine times and never included <stdarg.h>. On a recent
  # toolchain that header arrives transitively; on gcc 11 it does not, so
  # va_start became an implicit function call and the build failed at LINK. The
  # defect predated 0.1.0.
  #
  # NOTHING IN THE PROJECT COULD SEE IT. A 145-suite gate, the three jobs above
  # and the lean-build packaging suite all passed, because every one of them
  # varies which LIBRARIES are present and none varies how OLD THE COMPILER IS --
  # and the oldest leg here is Ubuntu 24.04 (gcc 13), still new enough to mask
  # it. Found by building a release tarball in a container, not by any test.
  #
  # Deliberately MINIMAL: the defect is in eval.o, which is compiled in every
  # configuration, so no optional dependency is needed to expose it and adding
  # them would only couple this job to 22.04's different package names.
  #
  # IT ALSO ASSERTS THE GLIBC FLOOR, because that is the claim the download
  # rests on: a lean binary built here must run on RHEL/Rocky/Alma 9 (2.34),
  # Ubuntu 22.04 LTS (2.35) and Debian 12 (2.36). A base image that raises the
  # floor should fail HERE rather than ship a tarball that stops starting on the
  # machines it was built for.
  # A DIFFERENT LIBC, which is a THIRD axis and the one that had a defect in it.
  #
  # The `full`/`minimal` jobs vary which LIBRARIES are present; `oldest-toolchain`
  # varies how old the COMPILER is. Neither varies WHICH C LIBRARY, and on
  # 2026-09-20 that axis turned out to be hiding a real one: musl does not define
  # REG_STARTEND, so the regex fallback ran a NUL-terminated regexec and stopped
  # at an interior NUL -- `contains(from_bytes([65,0,66]), regex("B"))` answered
  # FALSE, breaking PLAT-NUL's guarantee that a gBASIC string is counted bytes in
  # which NUL is content. Found by building for musl, by no test.
  #
  # THIS IS A PORTABILITY CHECK, NOT A RELEASE BUILD. The download is built in a
  # container against an older glibc (floor 2.34, which reaches RHEL 9, Ubuntu
  # 22.04 and Debian 12), and that stays. What this job buys is the axis, not an
  # artifact: it compiles the paths a glibc machine never compiles and runs the
  # suites that do not need a dynamic loader.
  #
  # DELIBERATELY LEAN, and that is not only to match the release tier: musl's
  # -nostdinc collides with the glibc include paths pkg-config hands back, so a
  # FULL build cannot go through musl at all. Nothing optional is installed.
  #
  # VALGRIND TIERS ARE EXPECTED TO FAIL HERE and the suite list reflects it --
  # valgrind intercepts malloc through the dynamic linker and a static binary has
  # none, so nine suites lose their memory tier. Those tiers are covered by every
  # other job; what is covered ONLY here is the libc.
  musl-static:
    name: a different libc (musl, static)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install musl and nothing optional
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential bison musl-tools

      - name: Build against musl, statically
        run: |
          # LEANNESS IS FORCED, not hoped for. musl's -nostdinc collides with the
          # glibc include paths pkg-config hands back, so ONE optional dev
          # library happening to be on the runner turns this job red for a
          # reason that is not a gBASIC defect. A pkg-config that always fails
          # makes every HAVE_* zero regardless of what is installed.
          mkdir -p "$RUNNER_TEMP/shim"
          printf '#!/bin/sh\nexit 1\n' > "$RUNNER_TEMP/shim/pkg-config"
          chmod +x "$RUNNER_TEMP/shim/pkg-config"

          # `CC := cc` in the Makefile BEATS the environment, so the override has
          # to be on the COMMAND LINE. `export CC=musl-gcc` silently does not
          # work and produces a convincing glibc binary -- which cost an evening
          # once already, and is why the next step asserts musl rather than
          # "statically linked".
          PATH="$RUNNER_TEMP/shim:$PATH" make CC=musl-gcc LDLIBS="-lm -static"

      - name: It must really be musl, not merely static
        run: |
          # ASSERTED, because "statically linked" is true of a glibc static build
          # too and is the check that was passed while the wrong thing was being
          # measured. glibc's static binary still carries _dl_init_paths.
          file gbasic
          readelf -h gbasic | grep -q 'UNIX - System V' \
            || { echo '::error::not a musl binary (OS/ABI is not System V)'; exit 1; }
          n=$(nm -a gbasic | grep -cE '_dl_init_paths|fillin_rpath' || true)
          [ "$n" = "0" ] \
            || { echo "::error::glibc internals present ($n symbols) -- this is a glibc static build"; exit 1; }
          ldd gbasic 2>&1 | grep -q 'not a dynamic executable' \
            || { echo '::error::not statically linked'; exit 1; }
          ./gbasic --version

      - name: The NUL guarantee, which is what this job exists for
        run: |
          printf 'print "finds B =" + string(contains(from_bytes([65,0,66]), regex("B")))\n' > /tmp/nul.bas
          out=$(GBASIC_PATH=stdlib ./gbasic /tmp/nul.bas)
          echo "$out"
          [ "$out" = "finds B =true" ] \
            || { echo '::error::PLAT-NUL broken on this libc: a regex stopped at an interior NUL'; exit 1; }

      - name: Suites that do not need a dynamic loader
        run: |
          status=0
          for s in run_negative run_regex run_core run_stridx run_recidx run_equality \
                   run_string_nul run_precedence run_relocatable run_arridx \
                   run_error_model run_warning_model run_continuation run_optparams \
                   run_for_each_index run_credit run_try_decode run_outline run_repl \
                   run_silent_traps; do
            [ -f "tests/$s.sh" ] || continue
            printf '\n===== %s =====\n' "$s"
            if ! timeout 1800 bash "tests/$s.sh"; then
              printf '::error::%s failed under musl\n' "$s"
              status=1
            fi
          done
          exit "$status"

  oldest-toolchain:
    name: oldest supported toolchain (Ubuntu 22.04, gcc 11)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: What toolchain is this
        run: |
          gcc --version | head -1
          ldd --version | head -1

      - name: Install only what is required
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential bison

      - name: Build
        run: make

      - name: The interpreter must work
        run: |
          ./gbasic --version
          printf 'program main()\n    print "hello from gcc 11"\nend program\n' > /tmp/smoke.bas
          timeout 60 ./gbasic /tmp/smoke.bas

      # The download tier promises this floor. Asserted, not assumed.
      - name: glibc floor must stay at or below 2.35
        run: |
          floor=$(objdump -T gbasic | grep -oE 'GLIBC_[0-9.]+' | sort -Vu | tail -1 | sed 's/GLIBC_//')
          echo "glibc floor: $floor"
          if [ "$(printf '%s\n2.35\n' "$floor" | sort -V | head -1)" != "$floor" ]; then
            echo "::error::glibc floor $floor is above the 2.35 the release tarball promises"
            exit 1
          fi

      # A relocated tree must find its own stdlib -- the property the download
      # rests on, exercised here on the oldest toolchain as well as the newest.
      - name: An installed tree runs from where it was put
        run: |
          make install PREFIX="$HOME/.local"
          mkdir -p /tmp/elsewhere && cd /tmp/elsewhere
          printf 'load dates\nprint "stdlib resolved"\n' > t.bas
          env -u GBASIC_PATH "$HOME/.local/bin/gbasic" t.bas

  minimal:
    name: no optional modules
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      # Deliberately NOT installing pkg-config: without it the Makefile disables
      # every optional module, which is the leanest configuration anyone can
      # build and the one a source release must still work in.
      - name: Install only what is required
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential bison

      - name: Build
        run: make

      # A NON-INTERACTIVE smoke. Not the adventure example: at EOF on stdin it
      # loops rather than exiting, so it hangs the job until the runner's own
      # timeout -- measured, not guessed.
      - name: The interpreter must still work
        run: |
          ./gbasic --version
          printf 'program main()\n    print "hello from a minimal build"\nend program\n' > /tmp/smoke.bas
          timeout 60 ./gbasic /tmp/smoke.bas

      # The suites must SKIP the module-dependent cases, not fail them. This is
      # the tier that would have caught "34 of 182 examples fail on a build with
      # no optional dependencies", which was true and unnoticed until 2026-08-15.
      - name: Suites must skip, not fail
        run: |
          # pipefail, not just -e: a suite piped into `tail` reports TAIL's exit
          # status, so a failing suite reads as a pass. That is how rc2 was
          # tagged while run_examples.sh was red (2026-08-20).
          set -eo pipefail
          bash tests/run_examples.sh | tail -5
          bash tests/run_negative.sh | tail -5
          bash tests/run_xlsx.sh
          bash tests/run_xlsx_cookbook.sh

  install:
    name: install to a prefix and run from it
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential bison pkg-config \
            zlib1g-dev libxml2-dev libsqlite3-dev

      # The stdlib path is COMPILED IN, so `make` followed by `make install
      # PREFIX=...` used to install a binary still pointing at /usr/local. It
      # failed silently: `load` either failed later for no visible reason, or
      # resolved against a different gBASIC's stdlib. Build first, deliberately,
      # so this job exercises that exact sequence.
      - name: Build, then install elsewhere
        run: |
          make
          make install PREFIX="$HOME/.local"
          make install-lsp PREFIX="$HOME/.local"

      - name: The installed binary must find its own stdlib
        run: |
          mkdir -p /tmp/elsewhere && cd /tmp/elsewhere
          printf 'program main()\n    load stats\n    print "stdlib resolved"\nend program\n' > t.bas
          env -u GBASIC_PATH "$HOME/.local/bin/gbasic" t.bas
          test -f "$HOME/.local/share/doc/gbasic/LICENSE"
          test -f "$HOME/.local/share/doc/gbasic/NOTICE"

      - name: Uninstall must leave nothing behind
        run: |
          cd "$GITHUB_WORKSPACE"
          make uninstall PREFIX="$HOME/.local"
          ! test -e "$HOME/.local/bin/gbasic"
          ! test -e "$HOME/.local/share/gbasic"
          ! test -e "$HOME/.local/share/doc/gbasic"

  # ---------------------------------------------------------------------------
  # macOS: AN EXPLORATION, NOT A GATE.
  #
  # gBASIC has never been built on a Mac. docs and README say Linux only, and
  # that claim has never been MEASURED -- nobody here owns a Mac. A hosted
  # runner is how it becomes a fact instead of a caveat, and the answer decides
  # something real for the book: if a Mac reader can have a NATIVE BINARY, the
  # whole VM-appliance question goes away for Volume 1.
  #
  # `continue-on-error` so the steps fail HONESTLY while master stays green.
  # This job's output is a report, and a report that cannot fail says nothing.
  #
  # MEASURED FROM THE SOURCE before writing it, so the log can be read against a
  # known list -- the Linux-specific surface is THREE mechanisms in two files:
  #   SOCK_SEQPACKET      actor mailboxes; AF_UNIX has no SEQPACKET on macOS,
  #                       and SOCK_DGRAM is the candidate (it keeps message
  #                       boundaries, which is the property relied on)
  #   /proc/self/exe      re-exec path; macOS is _NSGetExecutablePath()
  #   PR_SET_PDEATHSIG    "nothing this interpreter started outlives it", which
  #                       docs/reference.md states in bold and
  #                       run_process_lifetime.sh tests. macOS has no
  #                       equivalent; it needs kqueue NOTE_EXIT, and this is the
  #                       one with a design decision in it rather than a
  #                       mechanical substitution.
  # The event loop already uses poll(), not epoll -- which is usually the thing
  # that blocks a port, and it is already portable.
  #
  # ON DEMAND ONLY. macOS runner minutes bill at 10x the Linux rate against the
  # included quota for a PRIVATE repository (they are free for a public one). To
  # run it on every push to master, add `push:` back to `on:` below -- a
  # deliberate edit, not a default that surprises somebody with a bill.
  macos-exploration:
    name: macOS (exploration -- never gates)
    if: github.event_name == 'workflow_dispatch'
    runs-on: macos-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: What machine is this
        run: |
          uname -a
          sw_vers || true
          cc --version | head -3

      - name: Install dependencies
        run: |
          # macOS ships bison 2.3 (2006, GPLv2) and the grammar needs 3.x, so
          # the brew one has to come FIRST on PATH -- a build that silently used
          # /usr/bin/bison would fail with a grammar error that names the
          # grammar and not the tool.
          brew install bison pkg-config sqlite libxml2 openssl@3 zlib || true
          echo "$(brew --prefix bison)/bin" >> "$GITHUB_PATH"

      - name: Which bison
        run: |
          which bison
          bison --version | head -1

      - name: What does the build think it has
        run: ./tools/check-deps.sh || true

      - name: Build
        run: make

      - name: Does it run at all
        run: |
          ./gbasic --version
          printf 'print "hello from macOS"\n' > /tmp/hello.bas
          ./gbasic /tmp/hello.bas

      - name: The prompt
        run: printf 'x = 2\nprint x * 21\nquit\n' | ./gbasic --repl

      - name: Every suite
        run: ./tests/run_all.sh
