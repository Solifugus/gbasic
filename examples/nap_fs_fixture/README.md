Scratch fixture directory for examples/nap_fs_test.gb (NAP-10 filesystem
metadata + atomic_replace). The test creates and deletes its own files here;
only this README is tracked so the directory persists in git.
