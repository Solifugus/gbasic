# notesd configuration. Edited by an operator; dpkg will not overwrite it on
# upgrade (it is registered as a conffile).

# Loopback only. Exposing this service is the reverse proxy's job, not the
# application's -- see docs/shipping_applications.md.
port = 8099

database = /var/lib/notesd/notes.db
