# COOKBOOK — the one blessed gBASIC idiom per task

> **Placeholder (Phase D1).** This file is filled in **Phase D3**: one correct,
> runnable idiom per major construct and module (iterate an array, build/mutate a
> record, tolerate bad input, call each native module, etc.). **Every snippet must
> be a real file under `examples/`/`tests/` that this cookbook references** — no
> inline, untested code. Phase D3 also adds an executable-docs gate that runs every
> referenced sample so the cookbook cannot rot.

Until then, use the existing wired examples under `examples/` and `tests/` as the
idiom source (they are all suite-verified), and the human reference in
`docs/reference.md` / `README.md`.
