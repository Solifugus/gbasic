' The same defect with no remedy to offer: `foo` is not a word anyone means
' `function` by, so the message names the rule and stops. Its pair above is
' what shows the remedy is attached to the WORD and not to the rule.
foo bar()
  print "hi"
end foo
