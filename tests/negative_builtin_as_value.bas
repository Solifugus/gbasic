' A builtin used where a value belongs is not an "undefined variable": the
' language plainly knows the name, and saying otherwise sent readers looking
' for a spelling that was already right. gBASIC has no bare function
' references, so this is still an error -- the message just tells the truth
' about which kind.
print len
