' Ordering a month-bearing duration is refused, not answered: a month has no
' fixed length, so 1 month > 30 days has no true answer -- and before the §4.3
' respec, EVERY duration ordering silently returned false.
print (1 month) > (30 days)
