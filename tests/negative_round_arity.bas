' Zero arguments is still refused, and the message NAMES the optional second
' one -- "expects two arguments" would now be false, and a reader who has just
' been told `round(x)` works needs the message to say what a call may hold.
print round()
