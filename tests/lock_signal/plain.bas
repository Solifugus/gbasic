' THE CONTROL: the same shape with no lock. How a gBASIC program dies when
' nothing has installed a handler, which is what the locked one must match.
program main(args)
    print "holding"
    sleep(30)
end program
